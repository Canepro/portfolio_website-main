{
  "name": "portfolio_nextjs",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start"
  },
  "dependencies": {
    "next": "^12.0.10",
    "react": "17.0.2",
    "react-dom": "17.0.2",
    "react-icons": "^4.2.0",
    "styled-components": "^5.3.0",
    "styled-normalize": "^8.0.7"
  }
}

//Q: What is missing?
//A: The "next export" command is missing from the "build" script. This is the command that will generate the static files for the export. Without it, the build will fail.   

//Q: What is the difference between "next build" and "next export"?

//A: The "next build" command will generate the static files for the export. The "next export" command will generate the static files for the export and will also generate the static files for the dynamic routes.

//Q: How do you fix the error?
//A: Add the "next export" command to the "build" script.

//Q: Where can I find the "build" script?
//A: In the "package.json" file.

//Q: What is the command I need and where in the "package.json" file?



